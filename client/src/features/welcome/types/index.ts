export type Side = "left" | "right";

export type WelcomeFeature = {
  side: Side;
  title: string;
  text: string;
  src: string;
};

export type HeroStatItem = {
  label: string;
  value: string;
};
